# 多模态 RAG 进阶实验

这是第二阶段实验指导，尚未实现为本地可运行多模态服务。先完成本目录文本检索基线，再把下面四个环节逐个替换；无需重新创建一个名称不同、功能重合的项目。

## 可靠的教学入口

- [码士官方项目更新计划：基于 GME 的多模态 RAG＋上下文工程](https://www.mashibing.com/update/plan/845?month=2025-10)。官网列出了课程需求和技术方向，可作为项目范围依据，不能当成全部课件已开放。
- [课程目录：GME 多模态 RAG](https://www.mashibing.com/course/2973)。
- [阿里官方 GME 模型卡](https://huggingface.co/Alibaba-NLP/gme-Qwen2-VL-7B-Instruct)：按模型卡理解输入和推理方式。
- [Ragas 官方评估教程](https://docs.ragas.io/en/stable/getstarted/rag_eval/)。

## 四次递进实验

1. **保留文档结构。**自己制作 5 份含文字、表格、图表的脱敏/合成材料。为每个元素存 `document_id / page / bbox / type / text_or_asset`，先验收从引用能回到原页。
2. **区分 OCR 与视觉检索。**OCR 路线将图片转换为文字再检索；GME 路线直接对图文做表示。用“图表里谁增长最快”等问题比较两者，不能把 OCR 后文字检索命名为已完成 Any2Any。
3. **替换检索器。**保留当前 `retrieve` 对外证据格式，引入实际多模态模型及向量库。记录模型版本、维度和距离函数；不同版本向量不混用。先比较单路召回，再考虑融合和重排。
4. **单独评估。**标注正确页、正确表格/图片、回答依据。报告检索命中、引用定位、回答正确性、延迟和调用费用，并保留失败例。这里不提供未经运行的提升百分比。

完成标准：至少 20 道人工标注问题，覆盖纯文本、表格、图表、无答案四类；能现场演示一条普通 RAG 失败而多模态改进的案例，也能解释仍失败的案例。

本次没有下载模型权重或启用 GPU 服务。先据自己可用的硬件/服务确定模型方案，再运行官方推理示例。
